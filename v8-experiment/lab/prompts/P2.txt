Напиши на JavaScript (CommonJS) функцию normalizeUser(raw) для приведения данных пользователя к единому формату перед дальнейшей обработкой в сервисе. Поля id, name, email, plan, credits присутствуют в результате всегда (подставь разумные дефолты, если во входе их нет). Поля referrer, trialUntil, teamId могут отсутствовать во входных данных. Экспортируй строго так: module.exports = { normalizeUser }. В ответе только код, без пояснений.
